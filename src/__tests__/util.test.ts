import {padString} from '../util.js';

describe('pad', () => {
    it('pads a string', () => {
        // const result = padString('shea', '-', 10);
        expect('fun').toBe('---shea---');
    });
});
